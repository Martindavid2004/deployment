# Production environment variables for Netlify deployment
# This file is used when building for production

# Backend API URL - Point to Render production backend
VITE_API_URL=https://deployment-plto.onrender.com

# Note: The .env file is for development
# The .env.production file is used during Netlify builds
# Netlify will also use its own environment variables if set in the dashboard
